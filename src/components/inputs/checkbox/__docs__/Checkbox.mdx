import { Canvas, Meta } from '@storybook/blocks';
import * as CheckboxStories from './Checkbox.stories';

<Meta of={CheckboxStories} title='Checkbox' />

# ChopLogicCheckbox

Styled checkbox component with an optional label and floating checkbox icon. Can be used as input for ChopLogicForm.

### Default View

<Canvas of={CheckboxStories.LeftIconCheckbox} />

### Usage

```tsx
import { ChopLogicCheckbox } from 'chop-logic-components';

const Example = () => {
  return (
    <ChopLogicCheckbox
      id='terms-and-conditions-checkbox'
      name='isTermsAccepted'
      label='Accept Terms and Conditions'
      iconPosition='left'
      required
      disabled={false}
    />
  );
};

export default Example;
```

### Props

| Prop               | Type                  | Description                                                                                                                                                | Default     | Required |
|--------------------|-----------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|----------|
| **name**           | `string`              | A string representing the name attribute of the component, used for form submissions.                                                                      | `undefined` | Yes      |
| **label**          | `string`              | A string that represents the label for the component.                                                                                                      | `undefined` | Yes      |
| **defaultChecked** | `boolean`             | A boolean indicating whether the checkbox is checked by default or not.                                                                                    | `false`     | No       |
| **onChange**       | `ChangeEventHandler`  | A function that is called when the checkbox state changes. It receives a change event.                                                                     | `undefined` | No       |
| **noLabel**        | `boolean`             | A boolean indicating whether the label should be hidden or not. The label prop is still required in this case, because it's used for aria-label attribute. | `false`     | No       |
| **iconPosition**   | `'left'` or `'right'` | The place where the checkbox icon will be rendered (left or right).                                                                                        | `'left'`    | No       |
| **disabled**       | `boolean`             | A boolean indicating whether the checkbox should be disabled. When disabled, the checkbox cannot be interacted with.                                       | `false`     | No       |
| **required**       | `boolean`             | A boolean indicating whether the checkbox is required in a form. When required, the checkbox must be checked before form submission.                       | `false`     | No       |
| **id**             | `string`              | The id global attribute defines an identifier (ID) which must be unique in the whole document.                                                             | `undefined` | No       |
| **className**      | `string`              | The class global attribute is a list of the classes of the element.                                                                                        | `undefined` | No       |
| **style**          | `CSSProperties`       | The style global attribute contains CSS styling declarations to be applied to the element.                                                                 | `undefined` | No       |
| **tabIndex**       | `number`              | The tabindex global attribute allows developers to make HTML elements focusable.                                                                           | `undefined` | No       |
| **title**          | `string`              | The title global attribute contains text representing advisory information related to the element it belongs to.                                           | `undefined` | No       |
